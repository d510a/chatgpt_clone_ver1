# ========= 基本ライブラリ =========
streamlit==1.45.0
python-dotenv==1.0.1
openai==1.24.0

# ========= Word／Office 系 =========
python-docx==0.8.11     # .docx 読み取り
mammoth==1.6.0          # .docx → HTML
docx2txt==0.9           # テキスト抽出

# ========= PDF / OCR =========
textract==1.6.5         # 各種ファイル抽出のラッパー
pdfminer.six==20191110  # ← textract が要求する固定バージョン
PyPDF2==3.0.1
PyMuPDF==1.23.7
pdf2image==1.16.3
pytesseract==0.3.10
python-magic==0.4.27    # MIME 判定
Pillow==10.2.0          # 画像処理

# ========= データ処理 =========
numpy==1.26.4
pandas==2.2.2
lxml==5.2.1
httpx==0.27.0

# ========= textract が要求する下位依存 =========
six==1.12.0             # textract は >=1.12,<1.13 を要求

# ========= Streamlit の内部依存（明示するとビルドが安定） =========
altair<6,>=4.0
tornado<7,>=6.0.3
watchdog<7,>=2.1.5
gitpython!=3.1.19,<4,>=3.0.7
